const fs = require('fs');
const path = require('path');

console.log('Em dispositivos reais o AsyncStorage fica no app. Para resetar: limpe os dados do app no sistema ou reinstale o app.');
console.log('Este script existe apenas para documentar o fluxo de reset do MVP.');
console.log('Projeto:', path.resolve(__dirname, '..'));
